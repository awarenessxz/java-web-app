// Note: Have to change the way of implementing (this is temporary)
// eslint-disable-next-line import/prefer-default-export
export const checkUserAccess = (userId: string): boolean => {
    return false;
};
